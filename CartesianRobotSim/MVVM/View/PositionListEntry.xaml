<UserControl x:Class="CartesianRobotSim.MVVM.View.PositionListEntry"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CartesianRobotSim.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="533">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width ="*"/>
            <ColumnDefinition Width ="*"/>
            <ColumnDefinition Width ="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <!--X-Point-->
        <TextBlock Grid.Row="0" Grid.Column="0" 
                   Text="X" FontSize="16" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Right" VerticalAlignment="Center"
                   Margin="0,0,0,0"/>
        <TextBox Grid.Row="0" Grid.Column="1"
                 Text="{Binding XPosition}" FontSize="16"
                 Width="100" Height="25"/>

        <!--Y-Point-->
        <TextBlock Grid.Row="1" Grid.Column="0" 
                   Text="Y" FontSize="16" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Right" VerticalAlignment="Center"
                   Margin="0,0,0,0"/>
        <TextBox Grid.Row="1" Grid.Column="1"
                 Text="{Binding YPosition}" FontSize="16"
                 Width="100" Height="25"/>

        <!--Z-Point-->
        <TextBlock Grid.Row="2" Grid.Column="0" 
                   Text="Z" FontSize="16" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Right" VerticalAlignment="Center"
                   Margin="0,0,0,0"/>
        <TextBox Grid.Row="2" Grid.Column="1"
                 Text="{Binding ZPosition}" FontSize="16"
                 Width="100" Height="25"/>

        <!--AddButton-->
        <Button Grid.Row="3" Grid.Column="0"
                Content="Add" FontSize="16" 
                Click="Add_Click"
                Height="40" Width="100"
                Margin="0,10,0,0"
                HorizontalAlignment="Center" VerticalAlignment="Bottom"/>

        <!--RemoveButton-->
        <Button Grid.Row="3" Grid.Column="1"
                Content="Remove" FontSize="16" 
                Click="Remove_Click"
                Height="40" Width="100"
                Margin="0,10,0,0"
                HorizontalAlignment="Center" VerticalAlignment="Bottom"/>

        <!--SaveButton-->
        <Button Grid.Row="3" Grid.Column="2"
                Content="Save" FontSize="16" 
                Click="Save_Click"
                Height="40" Width="100"
                Margin="0,10,0,0"
                HorizontalAlignment="Center" VerticalAlignment="Bottom"/>

        <!-- Positions Added List -->
        <ListBox Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3"
                 ItemsSource="{Binding AddedPositions}" 
                 SelectedItem="{Binding SelectedPoint, Mode=TwoWay}"
                 DisplayMemberPath="Name"
                 Height="180" Width="500"
                 HorizontalAlignment="Center" VerticalAlignment="Center"
                 ScrollViewer.CanContentScroll="True"/>
                 <!--ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"-->

    </Grid>
</UserControl>
